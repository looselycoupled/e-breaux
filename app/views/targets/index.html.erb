<h1>Target List</h1>
<%= link_to 'Add Target', :action => :add %>


<ul>
  <% current_user.subscriptions.each do |s| %>
  <li><%= s.target.name %>
    <%= link_to "unsubscribe", :action => :unsubscribe, :id => s.id %>
  </li>
  <% end %>
</ul>